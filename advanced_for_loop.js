// advanced foor loop example

let arr = ["Keyboard", "Mouse", "Pen", "Pad", "Monitor", "iPhone"]
let i = 0, a
for (;;) {
    console.log(arr[i])
    i++
    if (i == arr.length) {
        break;
    }
}